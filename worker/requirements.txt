# Core dependencies
# IMPORTANT: For v0.2.5, using gRPC 1.78.0 to better align with Go gRPC 1.79.1
# Protobuf files will be regenerated with grpcio-tools==1.78.0
grpcio==1.78.0
grpcio-tools==1.78.0
# grpcio-tools 1.78.0 requires protobuf>=6.31.1
# This should be compatible with Go protobuf v1.36.10
protobuf>=6.31.1,<7.0.0

# ML/Transcription
faster-whisper==1.2.1  # Latest version with better PyAV compatibility
stable-ts-whisperless==2.17.5
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu  # CPU-only version for faster builds (~300MB vs ~900MB)
torchaudio==2.10.0+cpu

# Audio processing
ffmpeg-python==0.2.0
# av will be installed automatically by faster-whisper

# Configuration
pydantic==2.5.3
pydantic-settings==2.1.0
pyyaml==6.0.1
python-dotenv==1.0.0

# System monitoring
psutil==5.9.8

# HTTP health server
flask==3.0.0

# Logging
structlog==24.1.0

# Utilities
numpy==1.26.3
