# Core dependencies
grpcio==1.60.1
grpcio-tools==1.60.1
protobuf>=4.25.2,<6.0.0  # Allow newer protobuf versions for compatibility

# ML/Transcription
faster-whisper==1.2.1  # Latest version with better PyAV compatibility
stable-ts-whisperless==2.17.5
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu  # CPU-only version for faster builds (~300MB vs ~900MB)
torchaudio==2.10.0+cpu

# Audio processing
ffmpeg-python==0.2.0
# av will be installed automatically by faster-whisper

# Configuration
pydantic==2.5.3
pydantic-settings==2.1.0
pyyaml==6.0.1
python-dotenv==1.0.0

# System monitoring
psutil==5.9.8

# HTTP health server
flask==3.0.0

# Logging
structlog==24.1.0

# Utilities
numpy==1.26.3
